# Targets to delete on apply. chezmoi leaves unmanaged files alone, so removing
# a source file does not remove the target it used to render — a renamed or
# dropped skill, or a tool that moved to another install mechanism, has to be
# named here to clear it off every machine.
#
# Entries are one-shot: drop them once every machine has applied once, or they
# will re-delete those paths on a machine that later acquires them by other
# means. Currently empty, which is the resting state.
